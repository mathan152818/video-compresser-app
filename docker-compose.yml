services:
  video-compressor:
    build: .
    container_name: video-compressor
    ports:
      - "5000:5000"
    volumes:
      - ./temp:/app/temp
    restart: unless-stopped
    environment:
      - PYTHONUNBUFFERED=1
